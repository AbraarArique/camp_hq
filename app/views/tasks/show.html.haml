- content_for :title do
  %title #{@task.name} - #{@user.name} | CampHQ

.row
  .small-12.columns
    %h1.text-center= @task.name
    %h3.text-center Assigned to #{@user.name}
  .small-12.columns
    .user
      %p
        %b Description:
        #{@task.description}
      %p
        %b Completed:
        #{@task.completed}